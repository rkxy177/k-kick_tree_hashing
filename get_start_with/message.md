# 信息
 - 信息论下界：对于一个误报率为 $\epsilon$ 的过滤器，存储 $n$ 个元素所需的理论最小空间是$O\log_2(1/\epsilon)$位
 - \[B(U,V,n)=\log\binom{|U|}{n}+n\log |V|\]
 公式计算的是在理论上，存储 \(n\) 个键值对所需的最小比特数是多少。$U$ 是待存储的元素集合，所以 $|U|$ 表示不同的键的个数，V是存储的元素可取的值域，$n$ 是当前哈希表存储的元素个数，$B$ 是信息论下界
