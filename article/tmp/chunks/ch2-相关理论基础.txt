
--- P.17 ---
第二章 相关理论基础
2.1 哈希表基本概念
哈希表通过哈希函数ℎ ∶ 𝑈 → [𝑚]将键映射到有限个槽位置，支持插入、查
询和删除三种操作，理想情况下均为𝑂(1)期望时间。当ℎ(𝑘 ) = ℎ(𝑘 )且𝑘 ≠ 𝑘
1 2 1 2
时发生冲突，冲突处理策略直接影响哈希表在特定负载下的综合表现。
一个工程可用的哈希函数须满足确定性、均匀性和高效性三个准则。通用哈
希族[14]为均匀性提供了形式化定义：若对任意𝑥 ≠ 𝑦，Pr[ℎ(𝑥) = ℎ(𝑦)] ⩽ 1/𝑚，则
ℋ 为通用哈希族。本系统采用 splitmix64[15]作为最终化函数，其 64-bit 混
合步骤具有优异的雪崩效应，通过注入不同种子可生成多个统计独立的哈希函
数[14,16]。
负载因子𝛼 = 𝑛/𝑚是反映哈希表拥挤程度的核心参数，插入和查询的期望时
间复杂度均为𝛼 的单调递增函数。空间效率以每键浪费比特数𝑟度量：若存储𝑛
个元素的信息论最优空间为 ℬ 比特，实际占用 ℬ + 𝑟𝑛 比特，则浪费 𝑟 比特/键。
Bender等人方案[6]的核心贡献在于证明了𝑟可从Ω(loglog𝑛)压缩至𝑂(log(𝑘)𝑛)。
2.2 Cuckoo Hashing
Cuckoo Hashing[9]是一种经典的开地址哈希方案，其核心思想是每个键有 𝑑
个候选位置（通常𝑑 = 2），由𝑑 个独立哈希函数分别确定。当插入新键时：
1. 依次检查𝑑 个候选位置，找到第一个空位即插入
2. 若所有候选位置均被占用，随机选择一个候选位置上的现有键“踢出”（kick
out），新键占据该位置
3. 被踢出的键尝试在自己的其其他候选位置中寻找空位
4. 重复上述过程，直到所有键都找到位置或达到预设的最大踢出次数
CuckooHashing的优势在于查询严格为𝑂(1)（只需检查𝑑 个位置），但其负
7
--- P.18 ---
载因子通常受限（𝑑 = 2 时约 50%）。Arbitman、Naor 和 Segev[11] 进一步提出了
利用队列技术去摊还 Cuckoo Hashing 方案，通过队列化踢出操作缓冲区实现了
确定性最坏情况性能保证，利用全局预取技术[14]控制踢出链长度，摆脱了经典
方案对高概率分析的依赖。
2.3 k-kick 树哈希方案
Bender等人[6]提出的𝑘-kick树方案是对Cuckoo思路的重大推广。其核心创
新在于：
2.3.1 多层级 bin 划分
将哈希表的 𝑚 个槽组织为 𝑘+1 个层级（depth 0,1,⋯,𝑘）的 bin 层次结构。
Depth-0的bin包含全部𝑚个槽，depth-𝑖（𝑖 > 0）的bin大小为𝑠 = Θ((log(𝑖)𝑚)6)。
𝑖
随着深度增加，bin大小呈指数级递减。这种层次结构被称为𝑘-kick树。
2.3.2 优先级踢出策略
每个键 𝑥 被随机分配一个最大深度 𝑠(𝑥)，满足[6] Pr[𝑠(𝑥) < 𝑖] = 1/(log(𝑖)𝑛)2。
插入时按以下规则操作：
1. 新键𝑥试图在尽可能深的bin中寻找空位
2. 若目标bin无空位且未饱和（存在深度较低的键）：踢出该低深度键，新键
占据其位置
3. 被踢键保持原深度不变，在其自身深度bin中寻找新空位
4. 被踢键若触发新一轮踢出，则形成踢出链，但总踢出次数不超过𝑘
关键性质：高深度的键优先踢出低深度的键，被踢键保持原深度。每次插入
至多触发𝑘 次踢出，因此切换开销为𝑂(𝑘)。
2.3.3 理论保证
论文[6]证明，对于任意𝑘 ∈ [log∗𝑛]，上述方案可实现：
• 插入/删除时间：𝑂(𝑘)（高概率）
8
--- P.19 ---
• 查询时间：𝑂(1)（确定性）
• 每键空间浪费：𝑂(log(𝑘)𝑛)比特（高概率）
此结果打破了长期存在的 Ω(loglog𝑛) 空间下界，展示了时空权衡的极致曲
线：每次增加常数的更新时间，空间浪费可指数级减少。
9