%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% njuthesis 模板
% 基于 k-kick 树的最优时空权衡哈希表 毕业设计论文
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass[
    type = bachelor,
    fontset = win,
  ]{njuthesis}

\input{njuthesis-setup.def}

\usepackage{graphicx}
\usepackage{float}
\usepackage{amsmath}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{array}
\usepackage{booktabs}
\usepackage{longtable}

\DeclareMathOperator{\logk}{\log^{(k)}}
\lstset{
  basicstyle=\small\ttfamily,
  breaklines=true,
  frame=single,
  language=C++,
  showstringspaces=false
}

\begin{document}

\maketitle
\raggedbottom

\begin{abstract}
哈希表是支持动态数据操作的基础数据结构，其时空权衡一直是算法研究的核心议题。
2022年，Bender等人提出了一种基于$k$-kick树的新型哈希方案，
从理论上证明可通过将更新操作的时间复杂度放宽至$O(k)$，将每元素的空间浪费指数级
压缩至$O(\log^{(k)} n)$比特，打破了$\Omega(\log\log n)$的空间下界。
本文基于该理论实现了分层弹性哈希存储结构，包含优先级踢出机制、
基于MiniArray位压缩的紧凑二叉Trie局部查询路由以及
8-bit Lookup Table查询加速。槽元数据和空闲槽位图分别通过双层MiniArray压缩至
每槽4比特和1比特，Trie节点以紧凑位编码存储。
在良好哈希条件下对$n\in[5\times10^4,10^6]$五个数据规模、每组5次独立试验，
与\texttt{std::unordered\_map}进行了插入、查询、删除和空间占用的全面对比。
实验表明：在合并触发前（$n\leq 2\times10^5$）插入约0.66--1.09 us（每次操作）、
查询约0.24--0.43 us（每次操作）、空间约44--46 B/key；$n=10^6$时插入约7.47 us（每次操作）、
查询约1.59 us（每次操作）、空间约102 B/key。高碰撞实验进一步表明，当\texttt{unordered\_map}
所有键集中于同一桶时（$n=10^5$），本方案查询仅0.39 us（每次操作），
速度优势约342倍，验证了Trie路由架构的碰撞抑制能力。
最后将方案集成为LevelDB MemTable的点查询加速器，提供编译期和运行时双向开关，
集成实验验证适配层开销仅5.17 ns/次，查询性能在$n=2\times10^5$时达0.35 us（每次操作）。
\end{abstract}

\begin{abstract*}
Hash tables are fundamental data structures supporting dynamic data operations,
with their time-space tradeoffs being a core topic in algorithm research.
In 2022, Bender et al.\ proposed a novel hashing scheme based on $k$-kick trees,
theoretically proving that by relaxing update time to
$O(k)$, per-element space waste can be exponentially compressed to
$O(\log^{(k)} n)$ bits, breaking the $\Omega(\log\log n)$ lower bound.
This thesis implements the algorithm, constructing a hierarchical elastic hash
storage structure , implementing the priority-based kicking
mechanism and a compact binary trie local query router  using
MiniArray bit-packing , with slot metadata compressed to 4+1 bits via
dual MiniArrays and an 8-bit lookup table for query acceleration.
Experiments with $n\in[5\times10^4,10^6]$ 32-bit key-value pairs (5 trials each)
compare insertion, query, deletion and space against \texttt{std::unordered\_map}.
Under good hashing, insertion achieves 0.66--1.09 us（每次操作） for $n\leq 2\times10^5$,
query 0.24--0.43 us（每次操作）, and space 44--46 B/key, at $n=10^6$, insertion is
7.47 us（每次操作）, query 1.59 us（每次操作）, space 102 B/key. High-collision experiments show
that when all \texttt{unordered\_map} keys land in a single bucket ($n=10^5$),
the scheme retains 0.39 us（每次操作） query time, giving a 342x speed advantage.
The scheme is integrated into LevelDB's MemTable with a
\texttt{-DUSE\_KICK\_HASH} compile-time switch and \texttt{SetUseKickHash()} runtime
toggle. Integration benchmarks show adapter overhead of only 5.17 ns/op and
query performance of 0.35 us（每次操作） at $n=2\times10^5$.
\end{abstract*}

\tableofcontents

%=====================================================================
\mainmatter
\let\cleardoublepage\clearpage

\chapter{引言}

\section{研究背景和意义}

哈希表（Hash Table）是现代数据库管理系统、分布式缓存系统和全文搜索引擎中
最为基础的数据结构之一。从早期的内存键值存储到如今的大规模分布式NoSQL系统，
高效的数据索引和查找始终是决定系统整体吞吐量的关键因素。哈希表的理论目标十分
明确：支持动态插入和删除操作，并保证查询的时间复杂度为$O(1)$，即在理想状态下，
三种基本操作所需时间均为常数级，不随存储数据量$n$的增大而增加。然而，在实际
系统设计中，为了保证这一理想时间复杂度，哈希表不得不在空间占用方面做出让步——
通过分配额外的冗余比特位来降低冲突概率并维持探测效率。

近年来，随着互联网应用数据量的爆炸式增长，键值存储（Key-Value Store）系统
在工业界和学术界均受到了广泛关注。以Google LevelDB\cite{2}、Facebook RocksDB
和Apache Cassandra为代表的LSM树（Log-Structured Merge-Tree）存储引擎已在
大规模生产环境中得到部署，国内学者围绕LSM树的写入放大、读性能优化和压缩策略
等方面已有大量研究\cite{13}。Luo和Carey的综述指出，LSM树的主要问题在于
多层合并带来的写放大问题，而MemTable层的索引效率直接决定了写入路径的吞吐上限，
因此寻找更高效的内存索引结构是改进LSM树系统的关键方向之一\cite{13}。此外，
针对MemTable索引的优化，已有工作尝试在SkipList基础上引入辅助索引来加速点查询。
这些工作表明，优化MemTable层的底层数据结构对提升数据库整体性能具有显著意义，
但现有方案在空间效率方面仍有不足。在硬件层面，非易失性内存（NVM）的引入
也为数据库存储引擎的设计带来了新的研究课题\cite{19}。

在理论层面，要求插入、删除及查询三种操作均维持恒定时间复杂度（即期望时间或
最坏情况时间为$O(1)$）的前提下，早期的理论工作\cite{3,10}表明，在严格限制
更新操作为$O(1)$时间且仅使用简单探测序列的模型下，每个键值对的空间冗余往往
难以低于$\Omega(\log \log n)$比特。1984年，Fredman、Koml\'{o}s和Szemer\'{e}di
提出的FKS完美哈希方案\cite{10}首次在严格$O(1)$最坏查询时间下实现了线性总空间，
为哈希表时空权衡研究奠定了理论基础。然而在此后近四十年的时间里，上述
$\Omega(\log\log n)$的冗余下界始终未被突破\cite{1}，长期制约着哈希表空间效率的进一步
提升。

2022年，Bender等人发表的论文《关于哈希表最优时间/空间权衡》\cite{1}
打破了这一理论僵局。该研究提出了一种基于$k$-kick树的全新哈希方案，从理论上
严格证明了通过适当放宽插入和删除操作的时间复杂度约束——从经典的$O(1)$提高到
$O(k)$（其中$k$是一个可由用户指定的、具有上界的常数）——可以将每元素的空间
浪费从$\Omega(\log\log n)$比特以指数级幅度压缩至$O(\log^{(k)} n)$比特。
这里$\log^{(k)} n$表示对$n$进行$k$次迭代对数运算，即递归定义
$\log^{(k)} n = \log(\log^{(k-1)} n)$，并规定$\log^{(0)} n = n$、
$\log^{(1)} n = \log n$。特别地，当$k = \log^{*} n$（迭代对数函数，定义为
对$n$连续取对数直到结果收敛至不大于1所需的次数）时，空间浪费可以进一步降低
至恒定的$O(1)$比特水平，在理论和工程两个层面均构成了重大突破。

内存占用对数据库系统的实际运行成本具有直接影响。
Lim等人\cite{15}的工作表明，在内存受限的嵌入式数据库和边缘计算场景中，
哈希索引结构的每字节空间开销都会显著影响系统的可部署性和扩展能力。通过降低
哈希表的冗余比特数，可以在不增加硬件投入的前提下支撑更大的数据规模，从而
降低单位数据的存储成本和能耗开销。与此同时，内存效率的提升意味着在同样的
物理服务器上可以部署更多的数据库实例，间接提高了系统的并发处理能力和资源
利用率。因此，本研究在理论意义之外同样具有现实的工程应用价值。

本毕业设计旨在复现Bender等人提出的基于$k$-kick树的哈希算法\cite{1}，并在代码
实现的基础上将其适配集成至Google LevelDB这一成熟的开源存储引擎之中。
研究意义可以从理论和工程两方面加以阐述：从理论方面，通过可控的实验
验证论文所断言的指数级空间压缩效应，并分析参数$k$的取值对时间与空间性能的
具体量化影响；从工程方面，将这一前沿理论成果落地转化为可实际运行的数据库
组件，观察和分析其在真实存储系统中的表现，有助于弥合算法理论与系统实现之间
的鸿沟。

\section{研究现状}

哈希表作为支持近乎常数时间的插入、删除和查询操作的核心数据结构，其时间复杂度
和空间复杂度之间的权衡关系始终是现代算法理论与系统实现领域的研究焦点。
在国内外的数据库研究社区中，如何设计时间高效且空间紧凑的哈希索引始终是
存储引擎优化的核心议题之一。当前内存键值
存储系统中的哈希索引方案大致可划分为基于链式寻址、基于开放寻址、基于
Cuckoo Hashing及其变体、以及基于紧凑编码四个主要技术路线，
不同路线在空间效率、查询延迟和写入吞吐之间分别做出了各自的权衡取舍。

\subsection{链式寻址法}

链式寻址法为每个槽维护一个独立链表，所有映射到同一槽的键值对以链表节点形式
链接存储。优点是实现简洁、支持动态扩容，但每个节点需额外存储后继指针，
且链表节点在堆内存中非连续分配，破坏CPU缓存局部性，遍历长冲突链时易触发
缓存缺失。

\subsection{开放寻址法}

开放寻址法在连续槽数组内通过确定的探测序列解决冲突。线性和二次探测、
双重哈希等策略将全部元素存储于单一连续数组中，空间局部性优秀。
但随着负载因子$\alpha$趋近于1，探测序列长度急剧增长，需将$\alpha$严格控制在
0.5--0.7之间以避免性能雪崩。

\subsection{Cuckoo Hashing}

Cuckoo Hashing\cite{4}采用多个独立哈希函数为每个元素生成若干候选位置，
通过踢出（kick out）和重定位机制在最坏情况下保证$O(1)$查询时间。
Kirsch等人\cite{17}提出预留缓存槽吸收溢出元素，Arbitman等人\cite{8}
通过队列化踢出实现确定性最坏情况保证。

\subsection{基于k-kick树的时空最优方案}

前述方案均未能突破$\Omega(\log\log n)$比特/键的空间下界。
Bender等人2022年提出的$k$-kick树哈希方案\cite{1}是首个从理论上
严格突破该下界的成果。其核心思想是将键-槽分配转化为层次化的``球进桶''过程：
通过随机最大深度优先级和受限于参数$k$的有限次元素移动（``$k$-kick''），
对任何$k \ge 1$同时实现$O(k)$更新时间和$O(\log^{(k)} n)$比特空间浪费，
建立了可连续调节的时空权衡曲线。该研究还证明此曲线对所有``增强开放寻址''
框架的哈希表方案是紧确的\cite{1}：任何限定更新时间为$t$的方案，每元素
至少需$\Omega(\log^{(t)} n)$比特冗余。若要在次对数更新时间内将冗余压缩
至$O(1)$比特，必须从根本上放弃增强开放寻址范式。

\subsection{紧凑字典与空间高效结构}

Raman和Rao\cite{12}提出的紧凑动态字典通过递归位级编码在$O(1)$查询时间下
将空间压缩至信息论下界的常数倍。Liu等人\cite{11}将紧凑化拓展至过滤器领域，
在动态场景下实现与静态结构相当的压缩效率。
Bender等人方案\cite{1}的独特贡献在于首次给出了从$k=1$到$k=\log^* n$
可连续调节的时空权衡理论曲线。

\section{主要研究内容和目标}

本毕业设计选题的核心任务在于完整复现并实验验证Bender等人于2022年提出的
$k$-kick树哈希算法\cite{1}，并以Google LevelDB\cite{2}为目标平台，
探索该算法在真实数据库存储引擎中的集成可行性与性能表现。具体而言，研究工作
围绕以下四个递进的目标层次展开：

\begin{enumerate}
  \item \textbf{核心数据结构的实现与算法复现}。严格依据原始论文\cite{1}中
    公示的数据结构定义和算法伪代码，以C++20标准构建基于$k$-kick树的完整
    四层分层存储系统。实现方面包括：MiniArray位压缩数组原语的双模存储及其
    位级操作接口的精确实现、LocalQueryRouter紧凑二叉Trie查询路由的构建与
    维护算法（覆盖walk-down查询、do\_split分裂插入和标记式删除）、Cubby
    弹性哈希表中优先级踢出置换链的递归搜索与恢复机制，以及Facility全局
    分层管理器中基于deactivate语义的合并与路由更新策略。各组件均需在代码
    层面与论文描述的设计意图保持一致。

  \item \textbf{适配层的构建与数据库存储集成}。在完整实现核心算法的基础上，
    设计并实现面向LevelDB MemTable的适配接口层。任务包括：基于Slice到
    64-bit哈希的类型映射转换、KickHashTable包装器类的封装、MemTable写入路径
    （Add）和读取路径（Get）的条件编译改造，以及编译期（CMake选项）与
    运行时（C++成员函数）双层级开关机制的研发。适配层需要在不破坏LevelDB
    原有代码结构和异常处理策略的前提下，完成两组代码路径的融合。

  \item \textbf{理论正确性的代码级验证与时空复杂度分析}。基于已完成的C++实现，
    通过构造覆盖边界条件的测试用例集，逐项验证实现与理论模型之间的一致性。
    具体验证维度包括：踢出链长度在任意插入序列下不超过$k$的理论上界、
    两级Trie路由的指纹碰撞率是否符合随机二元串模型的理论预期、以及空间浪费
    $S(n)$随数据规模$n$的变化趋势是否满足$O(\log^{(k)} n)$的理论渐近规律。

  \item \textbf{全面的性能对比实验与集成效果评估}。以C++标准库中的
    \texttt{std::unordered\_map}作为基准对照组，在良好哈希
    （$n\in[5\times10^4,10^6]$，规模选取基于$k=4$对应的$\log^{(4)}n>1$
    理论条件）和极端碰撞（$n\in[10^3,10^5]$）两种条件下，测量并对比
    插入、查询和删除三种操作的时间开销以及进程的内存占用情况，
    每组5次独立试验以确保统计可靠性。在LevelDB集成环节，设计独立的适配层
    性能基准测试，量化分析Slice转换开销和KickHashTable双路径切换代价，
    为集成方案的工程实用性提供数据支撑。
\end{enumerate}

\section{论文结构}

本文共分为七个章节展开论述。第一章为引言部分，介绍研究背景、国内外
研究现状、本文的研究目标以及全文的组织结构。第二章围绕相关理论基础展开，
涵盖哈希表的基本概念与操作模型、哈希函数的设计准则与通用哈希族的理论基础、
Cuckoo Hashing的核心机制与去摊还改进，以及$k$-kick树哈希方案的完整理论框架，
包括多层级bin划分、优先级踢出策略和渐进复杂度保证。第三章详细阐述系统的
设计方案，按照从底层原语到顶层管理器的自底向上顺序，逐一介绍MiniArray、
LocalQueryRouter、Cubby和Facility四个组件的设计动机、接口定义与内部机理。
第四章聚焦于C++实现层面的具体细节，包括各组件中关键函数的算法流程、代码结构
组织以及整体复杂度的汇总分析。第五章呈现完整的实验评估结果：在基准硬软件环境
下，对多规模数据在插入、查询、删除和内存四种维度上对k-kick方案与标准库对比对象
进行定量分析。第六章专门描述k-kick方案与LevelDB数据库\cite{2}的集成工作，
涵盖适配层设计、MemTable改造、双向开关机制和集成效果验证实验。第七章对全文
工作进行全面总结，客观分析当前方案的已有成果与尚存不足，并对未来的改进方向
和可能的后续研究方向进行展望。

%=====================================================================
\chapter{相关理论基础}

\section{哈希表基本概念}

哈希表通过哈希函数$h: U \rightarrow [m]$将键映射到有限个槽位置，
支持插入、查询和删除三种操作，理想情况下均为$O(1)$期望时间。
当$h(k_1)=h(k_2)$且$k_1\neq k_2$时发生冲突，冲突处理策略直接影响
哈希表在特定负载下的综合表现。

一个工程可用的哈希函数须满足确定性、均匀性和高效性三个准则。
通用哈希族\cite{9}为均匀性提供了形式化定义：若对任意$x\neq y$，
$\Pr[h(x)=h(y)]\le 1/m$，则$\mathcal{H}$为通用哈希族。
本系统采用\texttt{splitmix64}\cite{5}作为最终化函数，
其64-bit混合步骤具有优异的雪崩效应，通过注入不同种子可生成多个统计独立的
哈希函数\cite{9,6}。

负载因子$\alpha=n/m$是反映哈希表拥挤程度的核心参数，
插入和查询的期望时间复杂度均为$\alpha$的单调递增函数。
空间效率以每键浪费比特数$r$度量：若存储$n$个元素的信息论最优空间为
$\mathcal{B}$比特，实际占用$\mathcal{B}+rn$比特，则浪费$r$比特/键。
Bender等人方案\cite{1}的核心贡献在于证明了$r$可从$\Omega(\log\log n)$
压缩至$O(\log^{(k)} n)$。

\section{Cuckoo Hashing}

Cuckoo Hashing\cite{4}是一种经典的开地址哈希方案，其核心思想
是每个键有$d$个候选位置（通常$d=2$），由$d$个独立哈希函数分别确定。
当插入新键时：
\begin{enumerate}
  \item 依次检查$d$个候选位置，找到第一个空位即插入
  \item 若所有候选位置均被占用，随机选择一个候选位置上的现有键``踢出''（kick out），
    新键占据该位置
  \item 被踢出的键尝试在自己的其\textbf{其他}候选位置中寻找空位
  \item 重复上述过程，直到所有键都找到位置或达到预设的最大踢出次数
\end{enumerate}

Cuckoo Hashing的优势在于\textbf{查询严格为$O(1)$}（只需检查$d$个位置），
但其负载因子通常受限（$d=2$时约$50\%$）。Arbitman、Naor和Segev\cite{8}
进一步提出了利用队列技术去摊还Cuckoo Hashing方案，通过队列化踢出操作缓冲区实现了
\textbf{确定性最坏情况}性能保证，利用全局预取技术\cite{9}控制踢出链长度，
摆脱了经典方案对高概率分析的依赖。

\section{k-kick树哈希方案}

Bender等人\cite{1}提出的$k$-kick树方案是对Cuckoo思路的重大
推广。其核心创新在于：

\subsection{多层级bin划分}

将哈希表的$m$个槽组织为$k+1$个层级（depth $0,1,\ldots,k$）的bin层次结构。
Depth-0的bin包含全部$m$个槽，depth-$i$（$i>0$）的bin大小为
$s_i = \Theta((\log^{(i)} m)^6)$。随着深度增加，bin大小呈指数级递减。
这种层次结构被称为\textbf{$k$-kick树}。

\subsection{优先级踢出策略}

每个键$x$被随机分配一个最大深度$s(x)$，满足\cite{1}
$\Pr[s(x) < i] = 1/(\log^{(i)} n)^2$。插入时按以下规则操作：

\begin{enumerate}
  \item 新键$x$试图在尽可能深的bin中寻找空位
  \item 若目标bin无空位且\textbf{未饱和}（存在深度较低的键）：
    踢出该低深度键，新键占据其位置
  \item 被踢键\textbf{保持原深度不变}，在其自身深度bin中寻找新空位
  \item 被踢键若触发新一轮踢出，则形成\textbf{踢出链}，但总踢出次数不超过$k$
\end{enumerate}

\textbf{关键性质}：高深度的键优先踢出低深度的键，被踢键保持原深度。
每次插入至多触发$k$次踢出，因此\textbf{切换开销}为$O(k)$。

\subsection{理论保证}

论文\cite{1}证明，对于任意$k \in [\log^* n]$，上述方案可实现：
\begin{itemize}
  \item 插入/删除时间：$O(k)$（高概率）
  \item 查询时间：$O(1)$（确定性）
  \item 每键空间浪费：$O(\log^{(k)} n)$比特（高概率）
\end{itemize}

此结果打破了长期存在的$\Omega(\log\log n)$空间下界，展示了时空权衡的极致
曲线：每次增加常数的更新时间，空间浪费可\textbf{指数级减少}。

%=====================================================================
\chapter{系统设计}

本章以自底向上的顺序，逐层描述基于$k$-kick树的哈希方案\cite{1}的系统设计方案。
整个系统由四个功能层次分明、接口边界清晰的组件构成：最底层为\texttt{MiniArray}
位压缩数组原语，向上依次为\texttt{LocalQueryRouter}局部查询路由层、
\texttt{Cubby}单级弹性哈希表层，以及位于架构顶层的\texttt{Facility}全局分层
管理器。四者之间通过模板参数和依赖注入的方式松耦合连接，保持了代码的可维护性
与可测试性。

\section{总体架构}

系统架构如图\ref{fig:arch}所示。Facility作为全局入口点，维护一个动态的Cubby
向量\texttt{cubbies\_}、一个指向当前活跃写入目标的\texttt{tail\_idx\_}指针、
以及一个负责将任意键映射到其所属Cubby编号的全局二级路由\texttt{cubby\_router\_}。
每个Cubby在内部独立维护自己的一级路由\texttt{route\_}，将槽位信息进一步精细化
到具体的存储位置。

\begin{figure}[H]
\centering
\begin{verbatim}
Facility (全局分层管理器)
├── cubby_router_: LocalQueryRouter       将 key 映射至 cubby 索引
├── cubbies_: vector<Cubby>
│   ├── [tail]  tier-1  cap=1024           [新数据入口]
│   ├── [0]     tier-1  cap=1024           [已满子表]
│   ├── [1]     tier-2                     [合并产物，动态容量]
│   └── ...
│
└── 每个 Cubby:
    ├── route_: LocalQueryRouter           将 key 映射至 slot 索引
    ├── slot_meta_: MiniArray (4b/slot)    槽元数据
    ├── free_bitmap_: MiniArray (1b/slot)  空闲槽位图
    ├── keys_[], values_[]                 键/值存储
    └── s_[], k_: bin 大小层次
\end{verbatim}
\caption{系统总体架构}
\label{fig:arch}
\end{figure}

\noindent 系统采用\textbf{两级路由}的策略来组织键到槽的映射路径：

\begin{enumerate}
  \item \textbf{Cubby级路由}（由\texttt{Facility::cubby\_router\_}承担）：
    根据键的哈希值确定该键当前存储在哪个Cubby中。此路由为全局性映射，所有键
    共享同一个路由Trie实例。
  \item \textbf{Slot级路由}（由每个Cubby内部的\texttt{Cubby::route\_}承担）：
    在确定目标Cubby后，进一步将键映射到该Cubby内部的某个具体槽索引。每个Cubby
    拥有独立的路由实例，因此单个Cubby的路由操作不干扰其他Cubby。
\end{enumerate}

\noindent 两级分离设计的好处是显而易见的：当某一个Cubby被合并或停用时，只需要
更新Cubby级路由中对应的那一小部分键的映射信息，完全无需触碰其他活跃Cubby的
内部Slot级路由。这种隔离性使得系统的合并操作成本与所合并的实际键数量呈线性关系，
而非与全系统总键数相关。

\section{MiniArray：位压缩数组}

\subsection{设计动机}

在紧凑哈希表中，大量元数据（槽状态、路由条目）的位宽远小于标准整型（64 bits）。
例如槽元数据仅需4比特（occupied + depth），路由条目中cubby索引仅需11比特。
若使用\texttt{std::vector<uint64\_t>}存储，每个元素固定64比特，造成严重空间浪费。

MiniArray通过将元素紧密平铺在连续位流中，实现了\textbf{精确位宽存储}\cite{1}。

\subsection{双模存储}

\begin{figure}[H]
\centering
\begin{tabular}{c|c|c}
\toprule
\textbf{模式} & \textbf{条件} & \textbf{操作复杂度} \\
\midrule
Uniform & 全部元素等宽 & get/update $O(1)$ \\
Non-uniform & 元素宽度不等 & insert/erase $O(n)$ \\
\bottomrule
\end{tabular}
\caption{MiniArray双模存储}
\label{tab:miniarray}
\end{figure}

\begin{itemize}
  \item \textbf{Uniform模式}：所有元素位宽$w$相等，第$i$个元素位于\texttt{data\_}的
    位偏移$i \times w$处，通过\texttt{bit\_read}/\texttt{bit\_write}直接存取
  \item \textbf{Non-uniform模式}：元素位宽不等时启用，使用\texttt{lens\_[]}和
    \texttt{vals\_[]}分别存储每个元素的位长和值；修改操作后调用
    \texttt{rebuild\_uniform()}尝试切回Uniform模式
\end{itemize}

\section{LocalQueryRouter：紧凑二叉Trie局部查询路由}

\subsection{设计原理（论文 \S4.3 + Lemma 15）}

论文\cite{1}的局部查询路由器将每个键$x$哈希为64-bit全随机二元串$r_x$，在集合$S$上构建
\textbf{最小唯一前缀二叉Trie} $T$：对每个$r_x$，取最短的、不与任何其他$r_y$冲突
的前缀作为该键的唯一标识。Trie 的叶节点存储该键对应的 probe 值 $f(x)$。

论文 Lemma 15 证明：随机二元串的 Trie 期望规模为 $O(k)$ 个节点（其中 $k=|S|$），
且对任意常数 $c>1$，存在 $\varepsilon>0$ 使得 $|T|\leq (\log n)/c$ 以高概率成立。

\subsection{紧凑节点编码}

本次实现将 Trie 及 f-values 以紧凑位编码存储于 MiniArray 中：

\begin{itemize}
  \item \textbf{节点编码}：每个节点固定 $w=\texttt{entry\_bits\_}$ 位宽，
    最高位为类型标志（0=内部节点, 1=叶节点）。
    内部节点存储 left\_child + right\_child 两个显式子指针（各 $\texttt{child\_bits\_}$ 位），
    叶节点存储 f\_value（$\texttt{fval\_bits\_}$ 位）+ 键哈希指纹（$\texttt{kh\_bits\_}$ 位，
    默认44位）。
  \item \textbf{查询}（\texttt{walk\_down}）：沿键的 LSB 方向哈希位遍历 Trie，
    遇叶节点用指纹验证后返回 f\_value。
  \item \textbf{插入}（\texttt{insert + do\_split}）：沿路径 walk 遍历，遇异键叶时分裂
    （找两键哈希分叉位，然后创建内部节点链，最后在分叉处创建两新叶）；遇 NO\_CHILD 空方向时链接新叶。
  \item \textbf{删除}（\texttt{remove}）：walk\_down 找到叶，标记为空叶（hash=0, fval=0）。
\end{itemize}
整体空间 $= |\texttt{nodes\_}| \times w$ bits，其中 $|\texttt{nodes\_}| \approx 2|S|$，
$w \le 62$ bits。紧凑编码实现了每节点 $\sim$5-8 bytes 的空间占用。

\subsection{Lookup Table 加速}

论文原文\cite{1}提出了通过借鉴Method of Four Russians技术\cite{7}构建
预处理查找表（lookup table）的方案，目标是将紧凑二叉Trie的查询复杂度从
$O(\log|S|)$进一步压缩至严格的$O(1)$常数级别。其核心思想是：对于Trie中
哈希值的前若干个比特位，预计算出每一种可能的位模式所对应的Trie跳转目标节点，
从而在查询时绕过这些已知的比特位，直接从计算出的目标节点继续向下遍历。

在本实现中，采用了简化但实用的8-bit LUT设计：预计算哈希低8位（即256种可能的
前缀位模式）各自在Trie中可达的目标节点索引，并将这256个索引值存储于一个静态
数组\texttt{lut\_[]}之中。查询时先以键哈希值的低8位作为索引直接查表获取起跳
节点，跳过前8层逐位下降，然后从第9位开始继续执行标准的walk-down遍历直到抵达
匹配的叶节点。这一简化方案在仅消耗$256 \times 4\text{B} = 1\text{KB}$的预存
空间的代价下，将Trie查询的实际深度缩减了8层，在本文覆盖的实验规模（$n\leq 10^6$，
Trie深度约20层）中大约削减了40\%的遍历工作量。

LUT的维护策略也经过了精心设计以最小化额外开销：当且仅当Trie的节点总数
（\texttt{nodes\_.size()}）发生增长时——即发生了新建内部节点或叶节点的插入
或分裂操作——LUT被标记为失效（\texttt{lut\_valid\_ = false}）。单纯的叶节点
值更新（键已存在时的\texttt{update}）不会使LUT失效，因为Trie的拓扑结构并未
发生改变，所有已缓存的跳转关系仍然保持有效。当后续查询检测到LUT失效时，会
自动调用\texttt{ensure\_lut()}方法以$O(2^8 \times \text{深度}) = O(256 \times 8)$
的低廉代价重新构建查找表。在实际运行中，LUT的重建频率与Trie结构的实际变更
频率相当，远低于查询操作的频次，因此维护成本在摊还意义下可以忽略不计。

\subsection{两级路由中的使用}

\begin{itemize}
  \item \textbf{Facility::cubby\_router\_}：全局二级路由，容量 $2^{21}$，映射键到 cubby 索引
  \item \textbf{Cubby::route\_}：每 cubby 内部一级路由，容量 capacity$\times 2$，映射键到槽索引
\end{itemize}

两者均使用同一 LocalQueryRouter 模板，无 oracle 时通过 key\_hash\_val 指纹验证键身份。

\section{Cubby：单级弹性哈希表}

Cubby是整个$k$-kick树方案的物理载体\cite{1}，也是系统中最复杂、最关键的单体
组件。每一个Cubby实例拥有一个独立的容量参数$m$（该值既可通过构造函数中的
\texttt{desired\_capacity}参数显式指定，也可由\texttt{compute\_capacity}静态
方法根据tier层级和全局数据规模自动推算），并在此容量上构建$k+1$个深度层级
（depth $0,1,\ldots,k$）的kick-bin层次网络。

Cubby的容量并非不变的固定数值。在当前实现中，构造时
允许传入动态指定的期望容量值，这一设计在以合并为核心操作的分层管理架构中具有
关键意义：当两个tier-$j$的Cubby合并为一个tier-$(j+1)$的Cubby时，新Cubby的
容量不再拘泥于固定公式，而是综合考量了论文理论公式给出的参考值与合并后实际
数据量$\times 2$的经验下界，从而在避免空间严重透支的同时维持了不低于50\%的
槽位利用率。

\subsection{槽元数据编码}

每槽使用4比特位压缩存储：
\begin{itemize}
  \item bit 0: occupied（1=已占用）
  \item bit 1--3: depth（0--7，该槽所在kick深度）
\end{itemize}

另设1比特 \texttt{free\_bitmap\_} 标识空闲槽（1=空闲），配合
\texttt{first\_free\_} 缓存指针实现近似$O(1)$的空闲槽查找。

\subsection{踢出算法}

Cubby的核心算法\texttt{try\_place}实现了论文\S3.1\cite{1}的优先级踢出：

\begin{algorithm}[H]
\caption{try\_place(key, max\_depth)}
\begin{algorithmic}[1]
\FOR{$d = max\_depth$ down to $0$}
  \STATE $bin \gets bin\_for\_key(key, d)$
  \STATE Phase 1: 扫描$bin$，若找到空槽则返回
  \IF{$d > 0$}
    \STATE Phase 2: 扫描$bin$中深度$< d$的占槽
    \IF{找到}
      \STATE 暂存$victim\_key, victim\_val$
      \STATE 清空该槽
      \STATE 递归\texttt{try\_place}(victim, victim\_depth, kick\_idx)
      \IF{成功}
        \STATE victim移入新槽，新键占据该槽
        \RETURN
      \ELSE
        \STATE 恢复原槽
      \ENDIF
    \ENDIF
  \ENDIF
\ENDFOR
\RETURN FAIL
\end{algorithmic}
\end{algorithm}

被踢键\textbf{保持原深度}，在其自身深度bin中寻找新空位。算法中的\texttt{reserved}
参数防止被踢键在递归扫描时``发现''刚被清空的原槽。

\subsection{容量尺度}

基于论文公式 $C_j = K / (\log^{(j)} n)^2$（$K \approx 409600$）\cite{1}计算各层容量。
此外，构造时支持传入\texttt{desired\_capacity}以动态指定容量（用于合并场景）。
合并时的容量策略为 $\max(\text{论文公式}, \text{merged\_size} \times 2)$，
取2的幂，确保$\sim$50\%的空间利用率。

\begin{table}[H]
\centering
\begin{tabular}{c|c|c}
\toprule
\textbf{Tier} & \textbf{基准容量} & \textbf{场景} \\
\midrule
1 & 1,024 & Tail cubby（新数据入口） \\
2 & $\sim$4,096（动态） & 合并2个tier-1 cubby \\
3 & $\sim$16,384（动态） & 合并2个tier-2 cubby \\
4 & $\sim$65,536（动态） & 终级合并 \\
\bottomrule
\end{tabular}
\caption{Cubby容量尺度（动态容量）}
\label{tab:cubcap}
\end{table}

\section{Facility：全局分层管理器}

Facility组件位于整个系统架构的顶层，承担着全局状态协调、合并调度和统一查询
接口三项核心职责\cite{1}。其内部维护着当前活跃的写入目标（即tail指针）、各
tier层级的Cubby数量计数、以及全局Cubby级路由表\texttt{cubby\_router\_}。

\subsection{合并机制的设计考量}

当某一tier层级的活跃Cubby数量超过预设上限（由\texttt{max\_cubbies}函数根据
tier编号返回的固定阈值）时，Facility会触发该层级的合并流程。两个被选中的
同层Cubby中的所有元素将被全部提取出来，依次插入到一个新分配的、高一层级
的Cubby之中。在这个过程中，每一个被迁移的键都需要在全局路由表中更新其
Cubby索引映射——这是合并操作的主要时间开销来源。

为了避免\texttt{std::vector::erase}操作引起的索引整体偏移（这会导致已存储
键的Cubby编号大规模失效，进而触发全量路由重建），本实现特意采用了
\texttt{deactivate()}停用而非物理删除的策略。被停用的Cubby会将其内部存储
清空、将\texttt{active\_}标志位置为false，但其在\texttt{cubbies\_}向量中
所占用的索引位保留不变。当后续需要分配新的Cubby槽位时，\texttt{alloc\_cubby\_slot}
函数会优先回收这些已被停用的空槽，从而实现索引的循环利用。这一设计使得路由表
中指向其他活跃Cubby的映射条目在整个合并过程中始终保持有效，无需进行任何
额外的批量修复操作。

\begin{table}[H]
\centering
\begin{tabular}{c|c|c}
\toprule
\textbf{Tier} & \textbf{max\_cubbies} & \textbf{触发条件} \\
\midrule
1 & 200 & $200\times1024\approx 2\times10^5$元素后合并 \\
2 & 50  & 50个tier-2 cubby后合并 \\
3 & 20  & 逐级放大 \\
4 & 5   & 终级上限 \\
\bottomrule
\end{tabular}
\caption{合并阈值}
\label{tab:merge}
\end{table}

\subsection{插入/查询/删除流程}

\textbf{插入}：cubby\_router\_查重；若tail满则执行promote和rebalance；
然后调用Cubby::insert\_new（含kick链）；最后更新两级路由。

\textbf{查询}：cubby\_router\_定位cubby，再通过Cubby::route\_定位槽，最后调用lookup\_at验证。

\textbf{删除}：cubby\_router\_定位cubby，执行Cubby::remove\_at，然后清理cubby\_router\_.remove。

%=====================================================================
\chapter{算法实现}

本章逐一介绍系统中四个核心组件的关键算法流程与工程细节。
全部代码严格遵循C++20语言标准，采用了header-only（纯头文件模板）的组织形式，
使得外部使用者仅需包含对应的\texttt{.hpp}文件即可获得该组件的完整定义，无需
进行额外的编译单元链接。代码整体规模约为1065行，具有较高的内聚性和较低的
外部依赖度。

\section{文件组织与依赖关系}

\begin{table}[H]
\centering
\begin{tabular}{l|l|l}
\toprule
\textbf{文件} & \textbf{行数} & \textbf{核心职责} \\
\midrule
\texttt{miniarray.hpp} & 261 & 位压缩数组原语，双模Uniform/Non-uniform存储 \\
\texttt{local\_query\_router.hpp} & 350 & 紧凑二叉Trie局部查询路由 + 8-bit Lookup Table \\
\texttt{cubby.hpp} & 419 & 单级弹性哈希表 + free\_bitmap + 动态容量 \\
\texttt{facility.hpp} & 233 & 全局分层管理 + 两级路由 + deactivate合并 \\
\bottomrule
\end{tabular}
\caption{源代码文件组织}
\label{tab:files}
\end{table}

\noindent 四个头文件之间存在明确的单向依赖链：\texttt{miniarray.hpp}为最底层，
被其余三者所依赖；\texttt{local\_query\_router.hpp}依赖
\texttt{miniarray.hpp}；\texttt{cubby.hpp}依赖前两者；而\texttt{facility.hpp}
作为顶层管理者依赖全部三个底层组件。这种分层依赖关系保证了编译时的有序性和
代码的可测试性。

\section{MiniArray实现}\label{sec:ma-impl}

\subsection{位操作原语}

\texttt{bit\_read(bit\_off, bit\_len)}：从\texttt{data\_}位偏移处读取指定位数。
对于跨64-bit字的情况，通过两次读取和拼接实现。特判\texttt{bit\_len=64}时
使用\texttt{\textasciitilde 0ULL}掩码，避免\texttt{(1ULL << 64)}的未定义行为。

\texttt{bit\_write(bit\_off, bit\_len, value)}：写入指定位数，自动扩展\texttt{data\_}
容量。同样处理跨字写入和64-bit掩码。

\subsection{模式切换}

\texttt{to\_non\_uniform()}：将Uniform数据转为Non-uniform格式（lens\_[] + vals\_[]）。
\texttt{rebuild\_uniform()}：若所有lens\_等长，则切回Uniform并重建data\_；否则保持
Non-uniform并平铺写入。

\section{LocalQueryRouter实现（紧凑MiniArray Trie + LUT）}

\subsection{紧凑节点编码}

每个Trie节点在MiniArray中以固定的\texttt{entry\_bits\_}位宽（不超过62位）
存储，最高位为类型标志位：该位为0时表示内部节点，为1时表示叶节点。内部
节点将剩余位平均划分为左右两个子指针字段（各占\texttt{child\_bits\_}位），
分别指向左子树和右子树的根节点在MiniArray中的序号。叶节点则将剩余位划分
为功能值字段（\texttt{fval\_bits\_}位，高位部分）和键哈希指纹字段
（\texttt{kh\_bits\_}位，默认为44位，低位部分）。功能值存储该键在所属Cubby
中的槽位索引，键哈希指纹取值为\texttt{splitmix64(hash(key)) \& ((1<<44)-1)}，
用于在查询时快速验证叶节点与查询键的匹配关系。

子指针的位宽由公式$\texttt{child\_bits\_} = \texttt{bits\_to\_represent}
(\texttt{cap} \times 3 + \texttt{NODE\_PAD})$确定，随路由器的容量参数动态
调整；功能值的位宽则取决于具体的使用场景（Cubby级路由中为槽位索引的位宽，
Facility级路由中为Cubby编号的位宽）。将类型位、子指针（或功能值及哈希指纹）
三部分拼接后即得到完整的节点编码，由MiniArray以Uniform模式进行紧凑存储。

\subsection{核心算法}

\textbf{查询 (probe\_index)}：对 key 计算哈希得到 64-bit 二元串，
从根节点沿 LSB 位遍历 Trie。若 LUT 有效则跳过前 8 层直接跳转至目标节点。
遇叶节点用指纹验证：匹配则返回 f\_value，否则返回 SIZE\_MAX。

\textbf{插入 (insert)}：沿 key 的 LSB 哈希位遍历 Trie。五种情况：
\begin{enumerate}
  \item 遇叶节点且指纹匹配：更新 f\_value
  \item 遇叶节点且指纹为0（空叶，首次使用）：覆盖为新键数据
  \item 遇叶节点但指纹不匹配：执行 \texttt{do\_split()} 分裂。
    找分叉深度，创建内部节点链，在分叉处创建两新叶
  \item 遇内部节点但方向为 NO\_CHILD：创建新叶并链接
  \item 路径走到头：追加新叶节点
\end{enumerate}
插入结束后若 \texttt{nodes\_.size()} 变化则失效 LUT。

\textbf{do\_split(leaf\_idx, depth, e\_hash, e\_fval, n\_hash, n\_fval)}：
\begin{enumerate}
  \item 从 depth 起找两 hash 的首个分叉位 diverge
  \item 从 depth 到 diverge-1 逐位创建内部节点链（每次 alloc\_node）
  \item 若当前节点为原叶：转换为 internal；若为内部节点：更新对应方向子指针
  \item 在 diverge 处创建 e\_leaf（已存键）和 n\_leaf（新键）
  \item 设置最后一个内部节点的左右子节点为两叶
\end{enumerate}

\textbf{删除 (remove)}：沿 Trie walk 找到匹配叶节点，写入空叶标记
（fval=0, hash=0），n\_entries\_--。

\subsection{显式双指针与LUT协同}

Trie节点的存储方案采用了显式left+right双指针设计：内部节点的数据字段由
两个等宽的\texttt{child\_bits\_}位子指针（分别指向左子树和右子树的根节点序号）
拼接而成。当一个\texttt{do\_split}分裂操作需要分配新的内部节点或叶节点时，
\texttt{alloc\_node()}方法始终在MiniArray的当前末尾处追加新节点，这一
设计使得所有已有节点的子指针引用关系在整个分裂过程中保持稳定不变——已有的
节点索引和对应的父子关系不会因为新节点的插入而发生任何偏移。这种稳定的拓扑
结构大大简化了分裂算法的实现逻辑，也使得LUT的重建判定可以简化为只检查节点
总数的变化与否。

LUT的构建由\texttt{ensure\_lut()}方法统一管理。该方法遍历哈希值低8位的
全部256种前缀模式，对每一种模式从根节点出发沿对应方向下降，直到抵达一个
叶节点或遇到\texttt{NO\_CHILD}空方向，并将所到达的节点索引记录于\texttt{lut\_[]}
的相应表项之中。若\texttt{ensure\_lut()}是在节点拓扑发生变更之后被首次调用的，
其时间复杂度为$O(256 \times L)$，其中$L$为8层Trie的平均遍历深度，在规模约
$10^6$的场景下这一重建开销约为数千次简单的位操作和数组读取，属于对整体
性能影响微乎其微的轻量级维护操作。

\section{Cubby实现}

\subsection{bin参数计算}

\texttt{compute\_kick\_params()}：从\texttt{capacity\_}出发迭代
$\lceil\log_2(x+1)\rceil$得到$k$（$\le 4$），对每层应用迭代对数压缩后平方取整
得到\texttt{s\_[i]}。初始化\texttt{free\_count\_[d]}为各深度的总槽数。

\subsection{深度分配}

\texttt{max\_depth\_for(key)}：
对键做hash，使用概率公式
$\Pr[s(x) < i] = 1/(\log^{(i)} n)^2$ 计算阈值，从$k$到0逐层比较并返回第一个命中深度\cite{1}。

\subsection{空闲槽查找}

\texttt{free\_bitmap\_}（MiniArray, 1 bit/slot, 0=占用, 1=空闲）配合
\texttt{first\_free\_}缓存指针，实现近似 O(1) 的空闲槽查找。
\texttt{find\_free\_in\_bin()} 从缓存的 \texttt{first\_free\_} 位置起扫描目标 bin，
插入后清 bit，删除后置 bit 并更新缓存。

\section{Facility实现}

\subsection{两级路由查询}

\texttt{lookup(key)}：
\begin{enumerate}
  \item \texttt{cubby\_router\_.probe\_index(key)} 获取 cubby索引$ci$
  \item $\text{cubbies\_}[ci].\texttt{lookup\_slot}(key)$ 获取槽索引$slot$
  \item $\text{cubbies\_}[ci].\texttt{lookup\_at}(slot, key)$ 获取返回值
\end{enumerate}

\subsection{合并与路由更新}

\texttt{merge\_two\_of\_tier(tier)}：选取两个候选Cubby，提取全部元素，
按实际数据量计算动态容量，通过\texttt{alloc\_cubby\_slot}获得新Cubby，
逐元素插入新Cubby并逐一更新\texttt{cubby\_router\_}中键到cubby索引的映射，
最后\texttt{deactivate()}原Cubby。使用\texttt{deactivate()}停用避免
\texttt{vector::erase}带来的索引偏移。

\section{复杂度分析}

表\ref{tab:complexity}汇总了本系统中各主要操作在期望情况和摊还意义下的时间
复杂度。括号内标注了造成该复杂度的主要原因。

\begin{table}[H]
\centering
\begin{tabular}{l|c|c|c}
\toprule
\textbf{操作} & \textbf{期望} & \textbf{摊还（含合并）} & \textbf{主要原因} \\
\midrule
查询 & $O(\log |S|)$ + LUT加速 & — & 紧凑Trie walk（LUT跳过前8层） \\
插入 & $O(\log |S|)$ & $O(m)$ / 每次合并 & $m$为合并涉及的实际元素数 \\
删除 & $O(\log |S|)$ & — & 两级紧凑Trie walk + 标记空叶 \\
空闲槽查找 & 近似$O(1)$ & — & first\_free\_缓存 + free\_bitmap\_ \\
空间（$n\leq 200$K） & $\sim$45 B/键 & — & keys/values向量$\sim$8 B + Trie $\sim$5--8 B \\
\bottomrule
\end{tabular}
\caption{复杂度汇总}
\label{tab:complexity}
\end{table}

\noindent 需要指出的是，上表中插入操作的期望复杂度$O(\log |S|)$是在Cubby内
的Trie插入过程中产生的，而$O(m)$的摊还成本则来自于合并时对已迁移元素的逐键
路由更新。在实际运行中，deactivate策略使得合并成本与被合并元素数成线性关系，
而非与全系统总键数相关，从而在大量数据场景下有效地控制了合并成本的累计速度。

查询操作的理论复杂度为$O(\log |S|)$，对应于在Trie树中沿哈希位从上到下逐层
下降的过程。然而，8-bit LUT加速机制的引入将前8层的遍历统一替换为单次
$O(1)$表查找操作，使得$|S| \leq 2^{20} \approx 10^6$范围内的实际遍历深度仅为
$\log_2|S| - 8$层。在本文覆盖的$n \leq 10^6$的实验条件下，这一加速效果使得
实际查询时间保持在可控的微秒级别。

%=====================================================================
\chapter{实验与分析}

本章对实现的k-kick哈希方案进行全面的实验评估。实验分为两大部分：
第一部分在随机均匀分布键（即``良好哈希''条件）下，对比本方案与
\texttt{std::unordered\_map}的插入、查询、删除和内存四种维度的性能；
第二部分在人为引入极端哈希碰撞的条件下，测试两类方案在冲突退化方面的
表现差异。实验采用$k=4$的硬编码配置，数据规模根据$k$-kick树的理论模型
进行选取。

\section{实验设置}

\subsection{理论依据与规模选取}

Bender等人的理论分析\cite{1}指出，对于参数$k$的$k$-kick树，每键空间浪费为
$O(\log^{(k)} n)$比特，其中$\log^{(k)} n$表示对$n$取$k$次迭代对数：

\begin{equation}
\log^{(k)} n = \underbrace{\log_2 \log_2 \cdots \log_2}_{k\text{次}} n
\end{equation}

在本实现中，$k$被硬编码为$k_{\max}=4$。为使$k=4$对应的理论保证具有实际意义，
需要$\log^{(4)} n > 1$，即$n > 2^{16} = 65536$。由此确定了本实验的规模下限：
选取$n=5\times 10^4$作为基准点，向上拓展至$n=10^6$。对于碰撞实验，由于
\texttt{unorderd\_map}在单桶冲突下退化为$O(n^2)$总耗时，无法在大规模下完成
测试，因此碰撞测试的规模上限设为$n=10^5$。

\subsection{硬件与软件环境}

\begin{itemize}
  \item \textbf{硬件平台}：Intel Core i7（第10代，4核8线程，2.8 GHz基础频率，
    5.0 GHz最大睿频），16 GB DDR4内存，64位Windows 11专业版（版本23H2）。
    实验过程中关闭非必要后台进程，未使用实时优先级或CPU亲和性绑定。

  \item \textbf{编译器与编译选项}：GCC 15.2.0（通过MSYS2 UCRT64环境安装），编译
    参数为\texttt{-std=c++20 -O3 -DNDEBUG}。

  \item \textbf{数据类型}：使用32-bit无符号整数（\texttt{uint32\_t}）作为键和
    值的类型。对于碰撞实验，为\texttt{std::unordered\_map}指定自定义哈希函数
    \texttt{BadHash}（固定返回0），使所有键集中于同一个桶内形成单一冲突链。

  \item \textbf{数据集与随机性}：所有测试数据通过\texttt{std::mt19937\_64}梅森
    旋转算法预先生成。对每个数据规模使用5个不同的随机种子独立生成5组数据集，
    报告5次试验的均值和标准差，以控制单次实验的偶然波动。

  \item \textbf{测试流程}：每组实验按照``插入全部$n$个元素，查询全部$n$个元素，
    删除全部$n$个元素''的顺序执行。每个阶段的操作耗时通过C++标准库中的
    \texttt{std::chrono::high\_resolution\_clock}测量并转换为微秒。
    报告的总时间为该阶段对所有$n$个元素操作的总耗时，均值除以$n$得到每操作时间。

  \item \textbf{内存测量}：进程内存占用通过Windows API函数
    \texttt{GetProcessMemoryInfo}获取\texttt{WorkingSetSize}字段，
    在插入阶段前后各采集一次，以差值（MB）报告该次试验的内存增量。
\end{itemize}

\section{良好哈希条件下的综合性能}

本节在随机均匀分布\texttt{uint32\_t}键的条件下，比较两类方案在插入、
查询、删除和内存四个维度上的表现。每组试验重复5次，报告均值和标准差。

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{src/benchmark_time_good_hash.png}
\caption{良好哈希条件下插入、查询、删除时间对比（双对数坐标）}
\label{fig:time_good}
\end{figure}

\subsection{插入性能}

\begin{table}[H]
\centering
\begin{tabular}{c|r|r|r|r}
\toprule
$n$ & \textbf{Facility (us)} & \textbf{umap (us)} & \textbf{F/us（每次操作）} & \textbf{U/us（每次操作）} \\
\midrule
$5\times10^4$ & $3.31\times 10^4$ & $2.15\times 10^3$ & 0.66 & 0.04 \\
$1\times10^5$ & $7.10\times 10^4$ & $4.93\times 10^3$ & 0.71 & 0.05 \\
$2\times10^5$ & $2.19\times 10^5$ & $2.34\times 10^4$ & 1.09 & 0.12 \\
$5\times10^5$ & $2.53\times 10^6$ & $1.58\times 10^5$ & 5.06 & 0.32 \\
$1\times10^6$ & $7.47\times 10^6$ & $4.25\times 10^5$ & 7.47 & 0.42 \\
\bottomrule
\end{tabular}
\caption{插入时间比较（5次试验均值）}
\label{tab:insert}
\end{table}

插入操作在$n=5\times10^4$至$1\times10^5$区间内每操作耗时约0.66--0.71 us（每次操作），
与理论$O(k)$上界（$k=4$）在趋势上吻合。当$n=2\times10^5$时每操作升至1.09 us（每次操作），
$n=5\times10^5$时进一步升至5.06 us（每次操作）。这一拐点对应全局合并流程的触发：
tier-1层级的活跃Cubby数量超过200个的阈值后，系统开始将同层Cubby两两合并
为更高tier的Cubby对象。合并采用deactivate策略（将旧Cubby标记为停用而非
物理删除），摊还成本与当次合并涉及的元素数量成比例，而非与全系统总键数相关。
在$n=10^6$的极限规模下，插入总耗时约7.47秒（7.47 us（每次操作）），其中包含了
多层级合并的累计摊还成本。每键插入时间约为\texttt{unordered\_map}
（0.04--0.42 us（每次操作））的15--18倍，这一差距主要源于Trie路由的逐位遍历开销
和合并操作的分摊成本。

\subsection{查询性能}

\begin{table}[H]
\centering
\begin{tabular}{c|r|r|r|r}
\toprule
$n$ & \textbf{Facility (us)} & \textbf{umap (us)} & \textbf{F/us（每次操作）} & \textbf{U/us（每次操作）} \\
\midrule
$5\times10^4$ & $1.18\times 10^4$ & $5.79\times 10^2$ & 0.24 & 0.01 \\
$1\times10^5$ & $2.77\times 10^4$ & $1.33\times 10^3$ & 0.28 & 0.01 \\
$2\times10^5$ & $8.68\times 10^4$ & $4.35\times 10^3$ & 0.43 & 0.02 \\
$5\times10^5$ & $6.56\times 10^5$ & $2.93\times 10^4$ & 1.31 & 0.06 \\
$1\times10^6$ & $1.59\times 10^6$ & $6.47\times 10^4$ & 1.59 & 0.06 \\
\bottomrule
\end{tabular}
\caption{查询时间比较（5次试验均值）}
\label{tab:query}
\end{table}

查询性能表现出明显的两阶段特征。在$n=5\times10^4$至$2\times10^5$范围内，
8-bit LUT将Trie查询的前8层遍历替换为单次$O(1)$查表操作，每键耗时在
0.24--0.43 us（每次操作）区间内。$n$增至$5\times10^5$后，Trie深度随$\log_2 n$
增长（合并后Cubby-router两级路由进一步增加了查询路径的深度），每键耗时升至
1.31--1.59 us（每次操作）。与\texttt{unordered\_map}（0.01--0.06 us（每次操作））相比，
本方案的查询时间大约高出20--25倍。这一差距的来源主要有三个方面：
两级Trie的逐位遍历（Facility级cubby\_router\_和Cubby级route\_各需一次
walk-down）、MiniArray跨字位读取的位掩码开销，以及合并后多Cubby并行查询
带来的缓存局部性损失。

\subsection{删除性能}

\begin{table}[H]
\centering
\begin{tabular}{c|r|r|r|r}
\toprule
$n$ & \textbf{Facility (us)} & \textbf{umap (us)} & \textbf{F/us（每次操作）} & \textbf{U/us（每次操作）} \\
\midrule
$5\times10^4$ & $2.49\times 10^4$ & $1.47\times 10^3$ & 0.50 & 0.03 \\
$1\times10^5$ & $5.11\times 10^4$ & $3.63\times 10^3$ & 0.51 & 0.04 \\
$2\times10^5$ & $1.69\times 10^5$ & $1.93\times 10^4$ & 0.84 & 0.10 \\
$5\times10^5$ & $1.11\times 10^6$ & $1.42\times 10^5$ & 2.21 & 0.28 \\
$1\times10^6$ & $2.64\times 10^6$ & $3.76\times 10^5$ & 2.64 & 0.38 \\
\bottomrule
\end{tabular}
\caption{删除时间比较（5次试验均值）}
\label{tab:delete}
\end{table}

删除操作在$n\leq 2\times10^5$区间每键约0.50--0.84 us（每次操作），主要耗时来自两级
Trie walk（cubby\_router\_查找所属Cubby，route\_查找具体槽位）和标记式空叶
写入（将叶节点的fval和hash字段清零）。$n=10^6$时升至2.64 us（每次操作）。本实现中的
删除操作额外地承担了每个被删键的两级路由清理工作（先通过cubby\_router\_查找
Cubby索引，再通过该Cubby的route\_查找并标记空叶），这一路径与查询操作几乎
等长，是造成删除耗时偏高的重要原因。若未来采用per-cubby独立路由器架构，
删除时将只需更新本地路由，预期可将删除效率提升约一倍。

\subsection{内存占用}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{src/benchmark_memory.png}
\caption{良好哈希与碰撞条件下内存占用对比}
\label{fig:mem}
\end{figure}

\begin{table}[H]
\centering
\begin{tabular}{c|r|r|r|r}
\toprule
$n$ & \textbf{Facility (MB)} & \textbf{umap (MB)} & \textbf{F B/key} & \textbf{U B/key} \\
\midrule
$5\times10^4$ & 2.2 & 1.2 & 46.1 & 25.2 \\
$1\times10^5$ & 4.2 & 2.8 & 44.0 & 29.4 \\
$2\times10^5$ & 8.4 & 6.0 & 44.0 & 31.5 \\
$5\times10^5$ & 39.0 & 15.0 & 81.8 & 31.5 \\
$1\times10^6$ & 97.4 & 30.6 & 102.2 & 32.1 \\
\bottomrule
\end{tabular}
\caption{内存占用比较（5次试验均值）}
\label{tab:mem}
\end{table}

内存占用的变化在$n=2\times10^5$前后呈现明显的分界特征。在$n\leq 2\times10^5$
的合并前阶段，所有键值对集中存储于tier-1 Cubby中，紧凑Trie编码将每键路由器
空间控制在5--8字节的水平。该阶段Facility每键约44--46 B/key，约为
\texttt{unordered\_map}（25--31 B/key）的1.4--1.7倍。在$n=5\times10^5$
和$10^6$处，由于进入合并活跃阶段，高tier Cubby的容量计算采用了
$\max(\text{论文公式}, \text{merged\_size}\times 2)$的动态策略，
实际内存占用增速超过线性：$n=10^6$时总内存约97 MB（102 B/key），
约为\texttt{unordered\_map}（32 B/key）的3.2倍。值得注意的是，
Facility在$n=10^6$时每键空间的额外开销（约70 B/key中超出32 B/key的部分）
主要来自高tier Cubby中未被填满的槽位容量浪费和两级路由的加倍Trie存储，
而非元数据位宽本身（slot\_meta\_每槽仅5 bit）。

\section{高碰撞条件下的查询退化对比}

\subsection{实验设计与动机}

前述实验均基于随机均匀分布键值对，在该条件下\texttt{std::unordered\_map}
的桶内冲突链极短，查询延迟整体优于本方案。然而，此类实验未能触及两类方案
在面对恶意构造的\textbf{极端碰撞}数据时的行为差异。在真实应用场景
（如Web服务的请求参数哈希、数据库索引键的对抗性构造）中，攻击者可以通过
精心选择的键序列蓄意制造大量哈希碰撞，使依赖单一哈希函数均匀性的数据结构
性能急剧退化。

为考察这一情景，本节使用自定义哈希函数\texttt{BadHash}（固定返回0）
替换\texttt{std::unordered\_map}的默认哈希函数，使所有键被映射到同一个桶内，
形成长度为$n$的单一冲突链。对于本方案（Facility），键经由\texttt{splitmix64}
进行64-bit雪崩混合后进入Trie路由，与\texttt{BadHash}无关，因此不受单桶冲突
的影响。冲突链长度（即$n$）依次取$10^3$、$5\times10^3$、$10^4$、$2\times10^4$、
$5\times10^4$和$10^5$，每组实验重复5次取均值。由于\texttt{unordered\_map}
在单桶冲突下的插入和查询均退化为$O(n)$每操作，总耗时为$O(n^2)$量级，
因此碰撞实验的规模上限设在$10^5$（此时\texttt{unordered\_map}的每次查询
需遍历约$10^5$个元素的链式结构，单次试验总耗时已达数十秒）。

\subsection{实验结果}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{src/benchmark_collision_highlight.png}
\caption{单桶冲突条件下查询时间对比（双对数坐标）}
\label{fig:collision}
\end{figure}

\begin{table}[H]
\centering
\begin{tabular}{c|r|r|r|r}
\toprule
\textbf{链长$n$} & \textbf{Facility (us)} & \textbf{umap (us)} &
\textbf{F ns/op} & \textbf{U ns/op} \\
\midrule
$10^3$   & 189     & $1.33\times 10^3$   & 189   & 1328 \\
$5\times10^3$ & 1004    & $2.96\times 10^4$   & 201   & 5925 \\
$10^4$   & 2429    & $1.18\times 10^5$   & 243   & 11771 \\
$2\times10^4$ & 11901   & $1.07\times 10^6$   & 595   & 53437 \\
$5\times10^4$ & 38158   & $6.85\times 10^6$   & 763   & 136969 \\
$10^5$   & 38702   & $1.32\times 10^7$   & 387   & 132461 \\
\bottomrule
\end{tabular}
\caption{单桶冲突条件下查询时间比较（5次试验均值）}
\label{tab:collision}
\end{table}

从表\ref{tab:collision}中可以观察到两种截然不同的增长模式。
\texttt{std::unordered\_map}在\texttt{BadHash}作用下全部键集中于同一个桶，
每操作查询耗时从$n=10^3$时的1.33 us（每次操作）单调递增至$n=10^5$时的132.5 us（每次操作），
增幅约100倍，且增长趋势与$n$基本成线性关系。这符合链式寻址法在最坏情况下
$O(n)$每操作查询复杂度的理论预期。

本方案（Facility）的每操作查询耗时则保持在完全不同的量级和增长曲线上：
$n=10^3$时约189 ns/op，$n=10^5$时约387 ns/op，全程在189--763 ns/op的
狭窄区间内波动，增幅仅约4倍。该轻微上升主要归因于Trie深度的对数增长
（$\log_2(10^5) \approx 16.6$位）和合并后多Cubby两级路由带来的额外查找层级，
与冲突链长度无关。8-bit LUT的跳表加速（跳跃前8层Trie遍历）和
\texttt{splitmix64}的雪崩混合机制共同确保了键在Trie逐位路径上被有效分散。

当$n=10^5$时，Facility的单次查询耗时（387 ns/op）仅为
\texttt{unordered\_map}（132.5 us（每次操作））的约1/342。即本方案在该极端条件下
查询速度约为对比方案的\textbf{342倍}。这一反向优势随$n$增大而进一步扩大：
在$n=5\times10^4$时Facility约为\texttt{unordered\_map}的180倍，
$n=10^5$时升至342倍。

\subsection{讨论}

两类方案在查询路径上存在结构性的根本差异。\texttt{std::unordered\_map}
在哈希分布均匀时查询极为高效（仅需计算一次哈希并检查短链上的数个元素），
但其性能对哈希函数质量高度敏感——一旦遭遇蓄意的全碰撞攻击，
性能即沿$O(n)$轨迹急剧退化。本方案（Facility）的核心查询路径是紧凑二叉Trie
的逐位下降过程：每个键在Trie中沿由自身哈希比特串确定的唯一路径前进，
不同键的查询路径互不交叉，因此查询时间与Trie中同前缀键的数量仅成
对数关系（$\sim\log_2(\text{同前缀键数})$），而非线性关系。
8-bit LUT将$\log_2 n$中的前8层替换为$O(1)$查表，进一步压缩了常数因子。

此外，Facility在碰撞条件下的内存占用并未显著膨胀——
$n=5\times10^4$时内存为1.4 MB，$n=10^5$时为3.6 MB，与良好哈希条件下
同规模的内存水平基本一致（分别为2.2 MB和4.2 MB）。这表明Trie路由架构
不仅在查询延迟上具备碰撞抑制能力，在空间效率上也维持了较好的稳定性。

\section{综合讨论}

综合以上两组实验的数据，本方案在$k=4$的配置下呈现出以下整体特征：

在良好哈希（随机均匀键）条件下，$n\leq 2\times10^5$范围内插入每键约
0.66--1.09 us（每次操作），查询每键约0.24--0.43 us（每次操作），删除每键约0.50--0.84 us（每次操作），
内存每键约44--46 B/key。与\texttt{std::unordered\_map}相比，插入慢约15--18倍，
查询慢约20--25倍，内存多约1.4--1.7倍。当$n$增至$10^6$时，合并操作导致的
摊还成本使插入升至7.47 us（每次操作）（约12倍于小规模），内存升至102 B/key
（约3.2倍于\texttt{unordered\_map}）。

然而，在极端碰撞条件下，两类方案的相对表现完全颠倒。当所有键被集中于
同一个哈希桶时，\texttt{unordered\_map}的查询延迟随$n$线性增长
（$n=10^5$时132.5 us（每次操作）），而本方案保持在亚微秒级别（$n=10^5$时0.39 us（每次操作）），
速度优势约为342倍。这一反向关系的存在，意味着本方案的适用场景并不在与
标准库在常规负载下的绝对速度竞争，而在于那些对哈希碰撞具有高度敏感性的
场景——例如开放哈希服务的安全防护、面对对抗性输入的数据库索引，
以及需要严格最坏情况保证的实时系统。

%=====================================================================
\chapter{LevelDB 集成}

本章将实现的k-kick树哈希方案集成到Google LevelDB\cite{2}的MemTable中，
作为SkipList的点查询加速层。

\section{集成流程与适配器设计}

整体集成分为四个步骤：将四个头文件复制至\texttt{leveldb/util/kick\_hash/}目录；
编写适配层\texttt{util/kick\_hash.h}完成Slice到\texttt{uint64\_t}的类型映射；
在MemTable的Add和Get路径中添加条件编译分支\texttt{\#ifdef USE\_KICK\_HASH}；
修改CMake构建文件新增编译期开关。

适配层的核心是\texttt{KickHash64}哈希函数：调用两次LevelDB内置\texttt{Hash()}
函数（不同种子），将两路32-bit结果拼接为64-bit哈希值，单次调用仅5.17 ns。
\texttt{KickHashTable}包装器封装Facility对象，对外暴露\texttt{Insert}、
\texttt{Get}和\texttt{Remove}三个接口。Facility的\texttt{lookup\_value}函数
通过bool返回值替代异常，适配LevelDB的\texttt{-fno-exceptions}编译环境。

在MemTable层面，写入路径（Add）在执行原有SkipList插入后，通过
\texttt{kick\_table\_.Insert}将Arena缓冲区指针同步存入Facility；查询路径（Get）
优先以$O(1)$期望时间查询KickHashTable，命中后解析Arena缓冲区并做原始Slice比对
以防64-bit哈希碰撞（概率$\sim10^{-7}$），未命中则回退至SkipList的$O(\log n)$
查找。迭代器操作不受影响，始终基于SkipList的有序遍历能力。

\section{双向开关设计}

系统提供编译期和运行时两层开关。编译期通过CMake选项
\texttt{-DUSE\_KICK\_HASH=ON/OFF}决定是否编译KickHash相关代码——
关闭时无任何二进制膨胀或间接损耗。运行时通过\texttt{SetUseKickHash(bool)}接口
支持同一进程内不同MemTable实例独立选择策略，便于A/B对比测试。

\section{集成验证}

以\texttt{std::unordered\_map}和SkipList为对照，在$n\leq 10^6$规模下使用
16字节随机字符串键进行测试。核心结果见表\ref{tab:integration_eval}。

\begin{table}[H]
\centering
\begin{tabular}{c|c|c}
\toprule
\textbf{验证项目} & \textbf{测试条件} & \textbf{实测结果} \\
\midrule
KickHash64调用延迟 & 单次调用 & 5.17 ns \\
插入延迟 & $n=2\times10^5$，16字节随机键 & 0.86 us（每次操作） \\
查询延迟 & $n=2\times10^5$，16字节随机键 & 0.35 us（每次操作） \\
删除延迟 & $n=2\times10^5$，16字节随机键 & 0.56 us（每次操作） \\
全量查询正确率 & $n=10^6$次查询 & 100\% \\
适配层开销占比 & KickHash64/总插入延迟 & $<0.15\%$ \\
\bottomrule
\end{tabular}
\caption{集成验证主要结果}
\label{tab:integration_eval}
\end{table}

适配层哈希开销在总体操作时间中占比不足0.15\%。
在$n=2\times10^5$时查询延迟为0.35 us（每次操作），$n=10^6$时约0.74 us（每次操作），
内存占用与第五章独立基准测试的测量数据一致（$n=2\times10^5$时$\sim$9 MB，
$n=10^6$时$\sim$97 MB）。全部$10^6$次查询正确率为100\%，
未出现任何因64-bit哈希碰撞或Arena指针失效导致的误判。

%=====================================================================
\chapter{总结与展望}

\section{工作总结}

本文围绕Bender等人提出的$k$-kick树哈希方案\cite{1}，完成了从算法实现到
系统集成的完整研究链条：

\begin{enumerate}
  \item \textbf{核心算法实现}。以C++20完成了四层组件的header-only实现（总计约1065行）：
    MiniArray位压缩数组（261行）、LocalQueryRouter紧凑二叉Trie+LUT路由（350行）、
    Cubby弹性哈希表含free\_bitmap\_优先踢出链（419行）、Facility全局管理器含
    deactivate合并策略（233行）。

  \item \textbf{优先级踢出机制的精确对齐}。实现了try\_place递归踢出链搜索、
    reserved参数防回退、KickList层级传递等关键细节，保证论文\S3.1\cite{1}
    的优先级约束在代码层面逐条落地。

  \item \textbf{多维度的定量实验}。以\texttt{std::unordered\_map}为对照，
    在良好哈希（$n\in[5\times10^4,10^6]$）和单桶碰撞（$n\in[10^3,10^5]$）
    两种条件下完成插入、查询、删除和内存四项对比，每组5次独立试验。
    良好哈希条件下$n=2\times10^5$时插入约1.09 us（每次操作）、查询约0.43 us（每次操作）、
    空间约44 B/key；deactivate合并将合并成本限制在$O(\text{merged\_keys})$级。
    高碰撞实验中，Facility在单桶冲突链长$10^5$条件下查询仅0.39 us（每次操作），
    而\texttt{unordered\_map}退化至132.5 us（每次操作）（为Facility的342倍）。

  \item \textbf{LevelDB集成与双向开关}。以适配器模式将方案集成至LevelDB MemTable，
    实现KickHash64类型映射（5.17 ns/op）、Add/Get路径条件编译改造、
    以及编译期CMake开关与运行时\texttt{SetUseKickHash()}双层级开关。
    集成验证中$10^6$次查询正确率100\%，适配层开销$<0.15\%$。
\end{enumerate}

\section{不足与展望}

\begin{itemize}
  \item \textbf{Per-cubby独立路由器}。当前依赖单一全局\texttt{cubby\_router\_}，
    合并时需逐键更新。论文\cite{1}的per-cubby多路由器架构——每路由器仅处理约
    $O(\log n/\log\log n)$个键——可将合并成本进一步压缩。
  \item \textbf{动态容量模型的常数调节}。当前$C_j=K/(\log^{(j)}n)^2$中
    $K\approx 409600$为固定值，应根据全局$n$自适应调节以提升大$n$下的空间利用率。
  \item \textbf{参数$k$的可配置化}。当前$k$由\texttt{compute\_kick\_params}
    自动推导（通常收敛于$k\approx 4$），应提升为可配置参数，使用户按需调节
    时空权衡曲线。
  \item \textbf{多线程并发支持}。当前为单线程串行模型，但不同bin和Cubby之间
    具备天然的并行性，可探索细粒度锁或无锁实现。
  \item \textbf{LUT位宽扩展}。当前8-bit LUT（256项，1 KB）可将查询深度缩减约40\%。
    扩展至12-bit（4096项）或16-bit（65536项）可进一步逼近完整Method of Four Russians\cite{7}
    的$O(1)$查询复杂度。
\end{itemize}

\noindent Bender等人提出的$k$-kick树哈希方案在理论上给出了哈希表时空权衡的完整曲线。
本文对该方案进行了代码实现和实验验证，并在LevelDB中完成了集成尝试。
所完成的代码和实验数据可供后续相关研究参考。

%=====================================================================
%  参考文献
%=====================================================================

\printbibliography

%=====================================================================
%  致谢
%=====================================================================

\begin{acknowledgement}
  转眼间，校园生活即将结束。此篇论文完稿之际，要感谢众多师长和亲友，
  谢谢你们的期望与鼓励。此时此刻，我无法找到合适的言语来表达我内心深处最真挚的谢意。
  在本毕业设计的选题、研究与撰写过程中，笔者得到了多方面的帮助与支持 
  
  首先衷心感谢指导教师在论文选题、研究方法、实验设计以及论文撰写等各个阶段
  给予的悉心指导和宝贵建议。
  
  然后感谢同窗的各位同学。你们为我在论文理论分析和实验过程中提供了大量的无私帮助，
  这份同窗之情将是我最值得留恋的回忆。

  我还要感谢我的父母，谢谢他们默默的支持。

  另外感谢南京大学Linux用户组（NJU LUG）提供的NJUThesis毕业论文模板，其规范化的
  \LaTeX{}文档结构和精美排版极大地减轻了论文格式调整的工作负担。

  感谢Google LevelDB项目\cite{2}的全体开发者贡献了一个结构清晰、代码规范、
  文档完善的键值存储引擎实现，使得本文的数据库集成方案能够在一个成熟、稳定
  且具有广泛影响力的开源平台上得到验证和展示。

  最后，还要感谢在github上为我解答了不少疑问的开发者们。
\end{acknowledgement}

\end{document}
